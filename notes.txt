Plan:

- Create landing page (UI)
  - Bootcards?
  - Current goal partial
    - Rotary % indicator (current vs plan) (with mi)

- Goal UI
  - left selectable list (with stats)
  - right chart of selected goal
  - right (maybe more data)

- Goal chart min y = 0

- Background nike sync
  - using sync button

- Add index (start time) to NikeRun

- Minify/compile javascript/css

- Deployment:
  - keep-alive (new relic?)